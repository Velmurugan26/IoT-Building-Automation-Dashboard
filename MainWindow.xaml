<Window
    x:Class="BuildingMonitorApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BuildingMonitorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <Grid Background="#1A1A2E">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="650" Padding="0,10,0,10">

            <TextBlock Text="🏢 Building Monitor Dashboard" 
                       FontSize="30" FontWeight="Bold" 
                       Foreground="White" TextAlignment="Center" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <TextBlock Text="🔌 COM Port:" Foreground="#A0A0C0" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
                <ComboBox x:Name="PortSelector" Width="120" Margin="0,0,10,0"/>
                <Button x:Name="ConnectBtn" Content="Connect" Background="#2980B9" Foreground="White" Width="110" Click="ConnectBtn_Click"/>
            </StackPanel>

            <Border x:Name="AlarmBanner" Background="#D32F2F" CornerRadius="8" Padding="8" Margin="0,0,0,15" Visibility="Collapsed">
                <TextBlock Text="🚨 WARNING: ALARM IS ACTIVE 🚨" 
                           Foreground="White" FontWeight="Bold" FontSize="16" 
                           TextAlignment="Center" HorizontalAlignment="Center"/>
            </Border>

            <Border Background="#16213E" CornerRadius="12" Padding="16" Margin="0,0,0,15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="🌡️  Temperature" FontSize="18" Foreground="#A0A0C0" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TempText" Text="-- °C" FontSize="24" FontWeight="Bold" Foreground="White" Grid.Row="0" Grid.Column="1"/>
                    <Border Grid.Row="1" Grid.ColumnSpan="2" Height="1" Background="#2A2A4A" Margin="0,8,0,8"/>

                    <TextBlock Text="💧  Humidity" FontSize="18" Foreground="#A0A0C0" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="HumidText" Text="-- %" FontSize="24" FontWeight="Bold" Foreground="CornflowerBlue" Grid.Row="2" Grid.Column="1"/>
                    <Border Grid.Row="3" Grid.ColumnSpan="2" Height="1" Background="#2A2A4A" Margin="0,8,0,8"/>

                    <TextBlock Text="🎛️  Knob Angle" FontSize="18" Foreground="#A0A0C0" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="AngleText" Text="-- °" FontSize="24" FontWeight="Bold" Foreground="#F39C12" Grid.Row="4" Grid.Column="1"/>
                    <Border Grid.Row="5" Grid.ColumnSpan="2" Height="1" Background="#2A2A4A" Margin="0,8,0,8"/>

                    <TextBlock Text="👁️  Motion Sensor" FontSize="18" Foreground="#A0A0C0" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="6" Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="MotionText" Text="Waiting..." FontSize="20" FontWeight="Bold" Foreground="#A0A0C0" HorizontalAlignment="Right"/>
                        <TextBlock x:Name="MotionTimeText" Text="Last detected: --" FontSize="11" Foreground="#8080A0" Margin="0,2,0,0" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#16213E" CornerRadius="12" Padding="12" Margin="0,0,0,15" Height="180">
                <lvc:CartesianChart Series="{x:Bind ChartSeries}" 
                                    XAxes="{x:Bind XAxes}" 
                                    YAxes="{x:Bind YAxes}" 
                                    LegendPosition="Right"
                                    Background="Transparent"/>
            </Border>

            <TextBlock x:Name="StatusText" Text="Waiting for connection..." Foreground="Cyan" TextAlignment="Center" FontSize="13" Margin="0,0,0,15"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="AlarmOnBtn"    Content="🚨  ALARM ON"    Background="#C0392B" Foreground="White" FontSize="15" FontWeight="Bold" Height="45" HorizontalAlignment="Stretch" Grid.Column="0" Click="AlarmOnBtn_Click"/>
                <Button x:Name="AlarmOffBtn"   Content="✅  ALARM OFF"   Background="#27AE60" Foreground="White" FontSize="15" FontWeight="Bold" Height="45" HorizontalAlignment="Stretch" Grid.Column="2" Click="AlarmOffBtn_Click"/>
                <Button x:Name="ResumeAutoBtn" Content="🔄  RESUME AUTO" Background="#8E44AD" Foreground="White" FontSize="15" FontWeight="Bold" Height="45" HorizontalAlignment="Stretch" Grid.Column="4" Click="ResumeAutoBtn_Click"/>
            </Grid>

            <TextBlock x:Name="RawText" Text="" Foreground="#404060" TextAlignment="Center" FontSize="11" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
